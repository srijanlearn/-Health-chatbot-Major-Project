# HealthyPartner v2 — Dependencies
# No cloud API packages. Everything runs locally.

# ── Web Framework & Server ─────────────────────────────────────────────────────
fastapi
uvicorn[standard]
requests
aiofiles

# ── LLM Inference (Local) ─────────────────────────────────────────────────────
ollama                          # Ollama Python client
langchain
langchain-core
langchain-community             # ChatOllama adapter

# ── Document Processing & RAG ──────────────────────────────────────────────────
langchain-chroma
langchain-huggingface
langchain-unstructured
unstructured[pdf,ocr]
pypdf

# ── Vector Store & Embeddings ──────────────────────────────────────────────────
chromadb
sentence-transformers

# ── Enhanced Search ────────────────────────────────────────────────────────────
rank-bm25                       # BM25 keyword search

# ── System & Config ────────────────────────────────────────────────────────────
python-dotenv
psutil                          # Hardware detection
pyyaml                          # Configuration

# ── OCR ────────────────────────────────────────────────────────────────────────
easyocr

# ── Frontend ───────────────────────────────────────────────────────────────────
streamlit

# ── Messaging (WhatsApp/SMS) ───────────────────────────────────────────────────
# flask removed — all endpoints now on FastAPI (app/main.py)
twilio
python-multipart                # required for FastAPI form data (Twilio webhook)
